# Weather Trip Planner environment variables

# Prisma + SQLite connection string
DATABASE_URL="file:./dev.db"

# OpenWeather API key (used for geocoding + weather forecast)
OPENWEATHER_API_KEY="your-openweather-api-key"
